import Ember from 'ember';
import layout from 'ui-base-theme/templates/components/ui-menu';

const { Component } = Ember;

export default Component.extend({
  layout,

  tagName: '',

  kind: 'default'
});
