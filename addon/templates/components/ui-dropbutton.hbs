{{#component frame
  action=(hash
    showPopup=(action "showPopup")
    hidePopup=(action "hidePopup")
    togglePopup=(action "togglePopup")
  )
  classes=classes
  kind=kind
  size=size
  showPopup=showPopup
  states=states
  targetId=targetId
as |component|}}
  {{yield component}}
{{/component}}
