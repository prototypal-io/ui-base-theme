<div {{ui-root}}>
  <div class="wrapper">
    <div class="message">
      {{yield}}
    </div>

    {{#if action.close}}
      <div class="close" {{action action.close}}>
        {{ui-icon icon="close" class=(concat ui-prefix "--close--icon")}}
      </div>
    {{/if}}
  </div>
</div>
