{{#component frame
  action=(hash
    mouseEnter=(perform mouseEnter)
    mouseLeave=(perform mouseLeave)
  )
  classes=classes
  generatedId=generatedId
  kind=kind
  size=size
  states=states
  targetRouteName=targetRouteName as |component|}}
    {{yield component}}
{{/component}}
