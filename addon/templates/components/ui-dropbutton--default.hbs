{{yield (hash
  trigger=(component "ui-dropbutton---trigger"
    kind=kind
    popup=(hash
      show=action.showPopup
      hide=action.hidePopup
      toggle=action.togglePopup
    )
    size=size
    states=states
    targetId=targetId
  )
  popup=(component "ui-popup"
    kind="dropbutton"
    attachment="top right"
    targetAttachment="bottom right"
    targetId=targetId
    size=size
    showPopup=showPopup
    show=action.showPopup
    hide=action.hidePopup
    toggle=action.togglePopup
  )
)}}
