{{#component frame
  classes=classes
  states=states
  kind=kind
  ids=ids
  placeholder=placeholder
  onFocusIn=onFocusIn
  onFocusOut=onFocusOut
  value=value
  size=size as |component|}}
    {{yield component}}
{{/component}}
