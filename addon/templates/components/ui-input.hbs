{{#component frame
  classes=classes
  states=states
  kind=kind
  ids=ids
  placeholder=placeholder
  onFocusIn=(action "onFocusIn")
  onFocusOut=(action "onFocusOut")
  value=value
  error=error
  size=size as |component|}}
    {{yield component}}
{{/component}}
