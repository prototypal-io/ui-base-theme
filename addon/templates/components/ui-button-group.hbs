{{#component frame
  action=(hash
    registerButton=(action "registerButton")
  )
  buttonGroup=(hash
    firstChild=firstChild
    lastChild=lastChild
  )
  classes=classes
  firstChild=firstChild
  kind=kind
  size=size
as |component|}}
  {{yield component}}
{{/component}}
