{{#component frame
  classes=classes
  states=states
  kind=kind
  ids=ids
  placeholder=placeholder
  value=value
  onFocusIn=onFocusIn
  onFocusOut=onFocusOut
  size=size as |component|}}
    {{yield component}}
{{/component}}
