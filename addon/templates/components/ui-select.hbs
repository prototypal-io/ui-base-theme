{{#component frame
  classes=classes
  states=states
  kind=kind
  ids=ids
  placeholder=placeholder
  value=value
  onFocusIn=(action "onFocusIn")
  onFocusOut=(action "onFocusOut")
  size=size as |component|}}
    {{yield component}}
{{/component}}
