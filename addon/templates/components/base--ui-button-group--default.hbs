<div {{ui-root}} id="{{id}}">
  {{yield (hash
    button=(component "ui-button"
      buttonGroup=buttonGroup
      register=(action action.registerButton)
      kind=kind
      size=size
    )
  )}}
</div>
