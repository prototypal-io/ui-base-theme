{{#component frame
  classes=classes
  kind=kind
  states=states
  size=size
  ids=(hash
    label=generatedLabelId
    input=generatedInputId)
  input=(component input onFocusIn=(action "onFocusIn")
                         onFocusOut=(action "onFocusOut")) as |component|}}
    {{yield component}}
{{/component}}
