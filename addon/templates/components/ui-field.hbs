{{#component frame
  classes=classes
  states=states
  size=size
  ids=(hash
    label=generatedLabelId
    input=generatedInputId)
  input=(component input onFocusIn=(action "focusIn")
                         onFocusOut=(action "focusOut")) as |component|}}
    {{yield component}}
{{/component}}
