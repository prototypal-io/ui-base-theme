<div class=":component
            {{classes.size}}
            {{ui-state states 'focus'}}">
  {{yield (hash
    input=(component
      input
      ids=ids
      size=size)
    label=(component
      "ui-field--default-label"
      ids=ids
      size=size)
    states=states
    description=(component
      "ui-field--default-description"
      size=size)
    )}}
</div>
